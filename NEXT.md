# 다음에 할 일 (NEXT)

> 마지막 업데이트: **2026-08-11**
> 이 파일만 읽으면 어디까지 했고 뭘 하면 되는지 알 수 있게 정리한 문서입니다.

---

## 📌 한 줄 요약

**뼈대 완성 + 실제 AI 모델 연결까지 끝났습니다.** 지금은 오탐(잘못 잡는 것)을 줄이는 단계입니다.

| 항목 | 상태 |
|---|---|
| 크롬 익스텐션 (수집 → 블러 → 통계) | ✅ 동작 확인됨 |
| FastAPI 백엔드 | ✅ 동작 확인됨 |
| AI 모델 `smilegate-ai/kor_unsmile` | ✅ **연결 완료** |
| 오탐 줄이기 | 🔧 진행 중 |
| 블러 화면 눈으로 확인 | ⬜ 아직 |

---

## 🚀 매번 시작하는 법 (제일 자주 볼 부분)

### 1단계 — 백엔드 켜기

PowerShell 창을 열고 아래를 **순서대로** 붙여넣습니다.

```powershell
cd C:\Project_outlist_moderation\backend
..\.venv\Scripts\Activate.ps1
uvicorn main:app --port 8000
```

`Application startup complete.` 가 뜨면 성공입니다.
**이 창은 끄지 마세요.** 끄면 판정이 멈춥니다.

> 💡 `--reload` 는 붙이지 마세요. 코드가 바뀔 때마다 모델(500MB)을 다시 읽어서 매우 느려집니다.

확인:

```powershell
curl http://localhost:8000/health
```

기대하는 답:

```json
{"status":"ok","mode":"kor_unsmile"}
```

| mode 값 | 의미 |
|---|---|
| `kor_unsmile` | ✅ 정상. AI 모델이 돌고 있음 |
| `rule-based-fallback` | ⚠️ 모델 로딩 실패. 키워드 규칙만 동작 중 |

### 2단계 — 유튜브에서 확인

1. 유튜브 영상 열기
2. `F5` (새로고침)
3. 댓글까지 아래로 스크롤
4. 댓글 위 `정렬 기준` → **최신순** (더 거친 댓글이 나옴)
5. 크롬 오른쪽 위 **🧩 퍼즐 아이콘** → `YouTube 댓글 유해 모더레이션` 클릭

### ⚠️ 익스텐션 코드를 고쳤을 때는 한 단계 더

`extension/` 폴더 안의 파일을 고쳤다면 **새로고침만으로는 반영되지 않습니다.**

1. `chrome://extensions` 이동
2. 확장 카드의 **🔄 새로고침 버튼** 클릭
3. 그다음 유튜브에서 `F5`

> `backend/` 만 고쳤다면 → 백엔드 재시작 + 유튜브 `F5` 만 하면 됩니다.

---

## ✅ 지금까지 한 것

### 1. 뼈대 만들기 (완료)

- 크롬 익스텐션(MV3): 댓글 수집 → 블러 처리 → 사이드 패널 통계
- FastAPI 백엔드: `POST /moderate`
- 유튜브 실제 페이지에서 **댓글 286개 수집 확인**

### 2. 진짜 AI 모델 연결 (완료)

가짜 키워드 규칙을 스마일게이트의 공개 모델로 교체했습니다.

```
[이전] 단어 목록에 있으면 욕설      ← 제가 손으로 친 정규식 11줄
[현재] AI 모델이 문맥을 보고 판정   ← smilegate-ai/kor_unsmile
```

지금은 **2단 구조**로 돌아갑니다.

| 담당 | 카테고리 | 방식 |
|---|---|---|
| AI 모델 | 욕설, 혐오표현 | 문맥 이해 |
| 키워드 규칙 | 성희롱, 스팸 | 정규식 (모델에 해당 라벨이 없음) |

### 3. 오탐 1차 수정 (완료)

**문제**: 축구 댓글 `아니 이게 축구냐`, `아.쪽팔린다...` 가 욕설로 잡힘

**원인**: 모델 라벨 이름이 `악플/욕설` 입니다. **`악플`이 포함돼 있어서, 욕이 한 글자도 없어도 비난하는 어조면 반응합니다.**

**해결**: 라벨마다 다른 기준값을 적용했습니다.

```
악플/욕설  → 0.85 이상만 (시끄러운 라벨이라 기준을 높임)
혐오 8종   → 0.50 유지   (정확하지만 점수가 낮게 나옴)
```

> 💡 **왜 패널 슬라이더로는 안 되나요?**
> 슬라이더는 모든 카테고리에 같은 잣대를 적용합니다. 80으로 올리면 축구 댓글(67)은 사라지지만
> **진짜 혐오표현(`틀딱` → 77)까지 같이 놓칩니다.** 그래서 백엔드에서 라벨별로 나눴습니다.
> **슬라이더는 기본값 50에 두세요.**

### 4. DOM 재활용 버그 수정 (완료, 검증 대기)

**문제**: 깨끗한 댓글 `솔직히 아스날이 우승하는거 자체가 말이안되지` 가 **욕설 92%** 로 표시됨

**진단**: 모델을 직접 테스트해보니 이 문장의 점수는 **clean 85.78% / 악플 9.91%** 였습니다.
즉 **모델은 정상이었고, 화면에 붙이는 과정이 틀렸습니다.**

**원인**: 유튜브는 스크롤할 때 댓글 DOM 요소를 새로 만들지 않고 **내용만 갈아끼웁니다**(재활용).

```
[노드 A] "ㅅㅂ 진짜..."        → 욕설 92% 판정 → 블러 + 배지 붙임
    ↓ 스크롤 (유튜브가 노드 재사용)
[노드 A] "솔직히 아스날이..."   → 내용만 바뀜, 표시는 그대로 남음
    = 깨끗한 댓글에 "욕설 92%" 가 붙어버림
```

부작용이 두 개였습니다. 잘못된 배지가 남는 것 + **갈아끼워진 새 댓글은 아예 판정을 못 받는 것.**

**해결**: 신원 기준을 "DOM 요소"에서 **"본문 텍스트"**로 바꿨습니다.

- 요소를 다시 볼 때마다 저장된 텍스트와 현재 텍스트를 비교 → 다르면 재활용으로 판단하고 흔적 제거
- 판정을 화면에 붙이기 직전에 **한 번 더** 텍스트 대조 → 다르면 취소
- 덤: 같은 댓글 재요청 방지(캐시), 통계 중복 집계 방지, `간략히` 버튼 텍스트 제거

---

## 🐛 알려진 이슈

| # | 이슈 | 상태 | 비고 |
|---|---|---|---|
| 1 | 깨끗한 댓글에 엉뚱한 판정이 붙음 | 🔧 **수정함, 검증 대기** | 아래 "다음 할 일 A" 참고 |
| 2 | 악플/욕설 기준값 `0.85` 가 적정한지 불명 | ⚠️ 열림 | 관측 몇 건으로 정한 추정치 |
| 3 | 블러가 실제로 어떻게 보이는지 미확인 | ⬜ 열림 | 눈으로 본 적 없음 |
| 4 | 성희롱은 키워드 규칙뿐 | ⚠️ 열림 | 공개 데이터셋에 라벨이 없음 |
| 5 | `severity` 는 심각도가 아니라 모델의 확신도 | ⚠️ 열림 | 이름과 의미가 다름 |
| 6 | 상업적 이용 불가 (라이선스) | 🔴 **중요** | 맨 아래 참고 |

---

## 📋 다음 할 일

### A. 재활용 버그 수정 검증 ← **제일 먼저**

수정은 했지만 **아직 실제 화면에서 확인하지 않았습니다.**

1. `chrome://extensions` → 확장 카드 **🔄 새로고침**
2. 유튜브에서 `F5`
3. **`F12` → `Console` 탭** 열어두기
4. 댓글을 위아래로 여러 번 스크롤

판정된 댓글마다 콘솔에 이런 로그가 찍힙니다.

```
[모더레이션] 정상 0 — 솔직히 아스날이 우승하는거...
    보낸 텍스트  : 솔직히 아스날이 우승하는거 자체가 말이안되지
    받은 판정    : 정상 severity=0
    붙인 댓글    : 솔직히 아스날이 우승하는거 자체가 말이안되지
    일치 여부    : ✅ 일치
```

**확인할 것: `일치 여부` 가 전부 ✅ 인가?**
❌ 가 하나라도 있으면 아직 버그가 남은 것입니다.

> 로그가 너무 많으면 `extension/content.js` 32번째 줄 `const DEBUG = true;` 를 `false` 로 바꾸세요.

### B. 블러 눈으로 확인

유해 판정된 댓글이 실제로 어떻게 보이는지 아직 한 번도 못 봤습니다.

찾는 방법 — 댓글 목록에서 **빨간 테두리 + 흐릿한 글씨**를 찾으세요.

```
┃ ⚠ 혐오표현 (유해도 81) · 모델이 '연령' 라벨을 81.0%...   [원문 보기]
┃ ░░░░░░░░░░░░░░░  ← 흐리게 뭉개진 글씨
```

- `[원문 보기]` 누르면 선명해지고 버튼이 `다시 가리기` 로 바뀌는지
- 배지 문구가 실제 댓글 내용과 맞는지

> 유해 댓글이 잘 안 보이면 뉴스 채널의 정치·사회 이슈 영상 댓글을 최신순으로 보세요.
> 혐오표현이 가장 많이 남아 있는 곳입니다. (유튜브가 노골적인 욕설은 이미 걸러냅니다)

### C. 오탐 / 미탐 수집

A, B 를 하면서 이상한 걸 발견하면 **스크린샷**을 남기세요.

- 🔴 **오탐** — 정상인데 유해로 잡힌 것
- 🔵 **미탐** — 명백히 심한데 안 잡힌 것

미탐이 많으면 `backend/moderation.py` 의 `LABEL_THRESHOLDS` 에서 `0.85` 를 `0.75` 로 낮추면 됩니다.
오탐이 많으면 반대로 올립니다.

### D. (그다음) 댓글 수집기 만들기

판정 결과를 CSV 파일로 저장하는 기능. 이게 있어야:

- 오탐을 모아서 분석 가능
- 기준값을 감이 아니라 **실제 점수 분포**를 보고 결정 가능
- 성희롱 분류기 학습 데이터의 출발점

---

## 🔧 어디를 고치면 뭐가 바뀌나

| 하고 싶은 것 | 파일 | 위치 |
|---|---|---|
| 악플 판정 기준 조정 | `backend/moderation.py` | `LABEL_THRESHOLDS` |
| 성희롱·스팸 키워드 추가 | `backend/moderation.py` | `_RULES` |
| 블러 세기 / 배지 디자인 | `extension/content.css` | `.oml-blurred`, `.oml-notice` |
| 한 번에 보내는 댓글 수 | `extension/content.js` | `BATCH_SIZE` |
| 콘솔 로그 끄기 | `extension/content.js` | `DEBUG = false` |
| 패널 화면 구성 | `extension/sidepanel.html` | — |

---

## 🆘 문제가 생기면

| 증상 | 원인 / 해결 |
|---|---|
| 패널에 🔴 `백엔드 꺼짐` | PowerShell 창이 닫혔음 → 위 "1단계" 다시 실행 |
| `rule-based-fallback` 로 표시됨 | 모델 로딩 실패. PowerShell 창의 빨간 에러 메시지 확인 |
| 수집된 댓글이 계속 0 | 댓글까지 스크롤 안 함 |
| 코드를 고쳤는데 반영이 안 됨 | 익스텐션은 `chrome://extensions` 에서 🔄 필요 |
| 백엔드를 껐는데 8000 포트가 안 풀림 | 아래 명령으로 정리 |

```powershell
Get-NetTCPConnection -LocalPort 8000 -State Listen | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
```

> `--reload` 로 띄운 서버는 자식 프로세스가 살아남아 포트를 계속 잡고 **예전 코드로 응답**하는 일이 있습니다.
> "분명 고쳤는데 그대로다" 싶으면 이걸 의심하세요. (실제로 한 번 겪었습니다)

---

## 🔴 반드시 기억할 것 — 라이선스

`kor_unsmile` 모델의 학습 데이터인 **Korean UnSmile Dataset 은 CC-BY-NC-ND 4.0** 입니다.

- **NC** = 비영리만 (돈 받으면 위반)
- **ND** = 변형 금지

**개인적으로 쓰는 건 괜찮지만, 수익화하려면 반드시 먼저 문의해야 합니다.**
문의처: `smilegate_ai@smilegate.com`

> 모델 자체(가중치)의 배포 조건은 데이터셋과 다를 수 있다는 언급이 있으나 **확인되지 않았습니다.**
> 사업화를 검토한다면 이걸 가장 먼저 정리하세요.
